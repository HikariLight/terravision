const Layout = ({ children }) => {
    return (
        <div className="h-screen w-screen scroll-smooth bg-gradient-to-bl from-white to-emerald-100">
            {children}
        </div>
    )
}

export default Layout
